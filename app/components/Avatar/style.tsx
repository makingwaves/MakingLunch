import { StyleSheet } from 'react-native';

import { colors } from '../../config/styles';

export default StyleSheet.create({
    imageContainer: {
        position: 'relative',
        width: 80,
        height: 80,
    },
    imageStyles: {
        flex: 1,
        width: '100%',
        borderRadius: 25
    }
});
