import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    container: {
        flex: 0,
        flexDirection: 'column',
        marginHorizontal: 60,
        padding: 30,
        width: 250,
        marginBottom: 35
    }
});
